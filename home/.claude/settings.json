{
  "theme": "dark-ansi",
  "includeCoAuthoredBy": false,
  "statusLine": {
    "type": "command",
    "command": "input=$(cat); model=$(echo \"$input\" | jq -r '.model.display_name // empty'); used=$(echo \"$input\" | jq -r '.context_window.used_percentage // empty'); if [ -n \"$used\" ]; then printf '%s | ctx: %.0f%% used' \"$model\" \"$used\"; else printf '%s' \"$model\"; fi"
  }
}
